# Maestro workspace config. Used by `maestro test .maestro/` and by
# Maestro Cloud uploads.
#
# 2026-05-16 — trimmed to just the 3 booking flows owner cares about
# (Hotels / Flights / eSIM). The other tab smoke tests + the launch
# / onboarding flows were dropped after Maestro #6 surfaced that the
# iOS bottom-tab labels can't be matched by text from Maestro's
# accessibility tree without code changes to the React Native
# `<Tabs>` component (needs `tabBarTestID` or similar). Those flows
# can be re-added later once the testID work lands in
# `mobile/app/(tabs)/_layout.tsx`.

flows:
  - 03-search-tab-hotels.yaml
  - 04-search-tab-flights.yaml
  - 07-search-tab-esim.yaml
